/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexel.com",
            "example.com",
        ]
    }
}

module.exports = nextConfig
